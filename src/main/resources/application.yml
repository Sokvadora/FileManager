spring:
  main:
    banner-mode: 'off'

  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
#    password: sa

server:
  port: 8081
